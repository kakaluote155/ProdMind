# Evidence Graph

ProdMind will represent an engineer investigation as a small directed graph built from already-normalized evidence. The graph is an explanation layer, not a second source of truth.

The first milestone will connect user action, HTTP/trace evidence, failure evidence, diagnostic rule/root cause, and historical incident evidence into a deterministic graph that can be rendered by an engineer UI.
